import 'package:flutter/material.dart';

class FavoritedPage extends StatelessWidget {
  const FavoritedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorited'));
  }
}
